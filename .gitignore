node_modules/
dist/
*.tsbuildinfo

# Upstream docs fetched for reference while building
.reference/
.next/
out/
build/

# Local HydraDB store and cache (bind-mounted into the container)
.hydradb/

# Downloaded / derived datasets
data/
*.jsonl
*.jsonl.gz

.env
.env.local
*.log
.DS_Store
.turbo/
coverage/
